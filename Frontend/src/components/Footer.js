import styles from "../styles/components/footer.module.scss";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>&copy; 2025 PetMarket. Всі права захищені.</p>
        </footer>
    );
};

export default Footer;
